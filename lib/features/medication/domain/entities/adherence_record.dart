/// Adherence record for reporting.
///
/// MiniMax fills in: fields for adherence percentage, streak length,
/// date range, per-medication breakdown.
library;

/// Aggregated adherence statistics over a date range.
class AdherenceRecord {
  // TODO: MiniMax — define fields for adherence reporting

  const AdherenceRecord();
}
