/// Cycle prediction DTO for database serialization.
///
/// MiniMax fills in: fields matching the cycle_predictions Drift table.
library;

/// Data transfer object for cycle prediction persistence.
class CyclePredictionModel {
  // TODO: MiniMax — define fields matching cycle_predictions Drift table

  const CyclePredictionModel();
}
