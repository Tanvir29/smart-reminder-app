/// Widget displaying current streak count and status.
///
/// MiniMax fills in: flame icon, streak count, grace period indicator,
/// streak freeze status, animation on streak increment.
library;

import 'package:flutter/material.dart';

/// Displays the current streak with ADHD grace period awareness.
class StreakBadge extends StatelessWidget {
  const StreakBadge({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement streak badge widget
    return const SizedBox.shrink();
  }
}
