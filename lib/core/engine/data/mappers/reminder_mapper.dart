/// Bidirectional mapper: Reminder entity <-> ReminderModel DTO.
///
/// MiniMax fills in: toEntity(), toModel() methods with data validation.
library;

/// Maps between Reminder domain entity and ReminderModel DTO.
class ReminderMapper {
  // TODO: MiniMax — implement toEntity(ReminderModel) and toModel(Reminder)

  const ReminderMapper();
}
