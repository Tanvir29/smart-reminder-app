/// Use case: Award XP for a completed action.
///
/// MiniMax fills in: call() that creates an XpEvent, persists it,
/// checks for level-up, and triggers achievement evaluation.
library;

/// Awards XP points for completing health-positive actions.
class AwardXp {
  // TODO: MiniMax — inject GamificationRepository

  const AwardXp();
}
