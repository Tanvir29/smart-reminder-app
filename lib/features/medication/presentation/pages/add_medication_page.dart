/// Page for adding or editing a medication entry.
///
/// MiniMax fills in: form fields (name, dosage, frequency, times,
/// instructions, isCritical toggle), validation, save action
/// calling MedicationNotifier.addMedication() via ref.
library;

import 'package:flutter/material.dart';

/// Form page to create or edit a medication.
class AddMedicationPage extends StatelessWidget {
  const AddMedicationPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement add/edit medication form
    return const SizedBox.shrink();
  }
}
