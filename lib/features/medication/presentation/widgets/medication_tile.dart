/// Tile widget for a single medication in the list.
///
/// MiniMax fills in: icon, name, dosage, next dose time,
/// tap-to-expand details, swipe actions (edit/archive),
/// ADHD-friendly high-contrast styling.
library;

import 'package:flutter/material.dart';

/// Displays a single medication entry with quick actions.
class MedicationTile extends StatelessWidget {
  const MedicationTile({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement medication tile UI
    return const SizedBox.shrink();
  }
}
