/// Use case: Get adherence statistics for a medication over a date range.
///
/// MiniMax fills in: read-only query returning AdherenceRecord.
library;

/// Calculates adherence stats (read-only, no side effects).
class GetAdherenceStats {
  // TODO: MiniMax — inject MedicationRepository

  const GetAdherenceStats();
}
