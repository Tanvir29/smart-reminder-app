/// Widget for selecting symptom tags during cycle logging.
///
/// MiniMax fills in: chip-based multi-select for common symptoms
/// (cramps, headache, bloating, fatigue, etc.), custom symptom input.
library;

import 'package:flutter/material.dart';

/// Multi-select symptom chip selector for cycle logging.
class SymptomSelector extends StatelessWidget {
  const SymptomSelector({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement symptom selector widget
    return const SizedBox.shrink();
  }
}
