/// Use case: Record a dose as taken, missed, or skipped.
///
/// MiniMax fills in: call() that creates DoseRecord and updates
/// the linked reminder status.
library;

/// Records a dose event and syncs reminder status.
class RecordDose {
  // TODO: MiniMax — inject MedicationRepository + ReminderRepository

  const RecordDose();
}
