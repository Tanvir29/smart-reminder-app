/// Medication domain entity.
///
/// MiniMax fills in: all fields (id, name, dosage, frequency, instructions,
/// isCritical, iconName, colorHex, isActive, timestamps), copyWith, equality.
library;

/// Immutable medication entity.
class Medication {
  // TODO: MiniMax — define all fields per spec §8.1.1
  // Must include: profile_id, created_at, updated_at, sync_status, xp_value

  const Medication();
}
