/// Cycle entry DTO for database serialization.
///
/// MiniMax fills in: fields matching the cycle_entries Drift table.
library;

/// Data transfer object for cycle entry persistence.
class CycleEntryModel {
  // TODO: MiniMax — define fields matching cycle_entries Drift table

  const CycleEntryModel();
}
