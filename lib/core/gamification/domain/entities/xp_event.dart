/// XP event domain entity for the gamification engine.
///
/// MiniMax fills in: fields (id, profileId, action, xpAmount, timestamp),
/// XP calculation rules.
library;

/// Represents a single XP-earning event.
class XpEvent {
  // TODO: MiniMax — define fields per v2 gamification spec

  const XpEvent();
}
