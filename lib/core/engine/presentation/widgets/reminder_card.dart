/// Reminder card widget displayed in the home feed.
///
/// MiniMax fills in: status-colored card, title, time, action buttons
/// (Snooze/Done), ADHD-friendly high-contrast styling.
library;

import 'package:flutter/material.dart';

/// Displays a single reminder with status indicator and action buttons.
class ReminderCard extends StatelessWidget {
  const ReminderCard({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement reminder card UI
    return const SizedBox.shrink();
  }
}
