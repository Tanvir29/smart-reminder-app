/// Snooze dialog showing snooze count and next delay.
///
/// MiniMax fills in: current snooze_count, remaining snoozes,
/// next delay calculation, single "Snooze" button.
library;

import 'package:flutter/material.dart';

/// Dialog shown when user taps Snooze on a reminder.
class SnoozeDialog extends StatelessWidget {
  const SnoozeDialog({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement snooze dialog UI
    return const SizedBox.shrink();
  }
}
