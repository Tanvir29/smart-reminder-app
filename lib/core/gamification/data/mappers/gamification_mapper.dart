/// Mapper between gamification domain entities and Drift table rows.
///
/// MiniMax fills in: toEntity/fromEntity methods for XpEvent,
/// Achievement, and Streak, mapping between domain and data layers.
library;

/// Maps gamification entities to/from Drift table rows.
class GamificationMapper {
  // TODO: MiniMax — implement toEntity/fromEntity for XpEvent, Achievement, Streak

  const GamificationMapper();
}
