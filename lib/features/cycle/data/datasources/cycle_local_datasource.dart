/// Local datasource for cycle persistence via Drift DAO.
///
/// MiniMax fills in: CRUD operations delegating to CycleDao,
/// mapping between CycleEntryModel/CyclePredictionModel and Drift rows.
library;

/// Handles cycle data operations against the local Drift database.
class CycleLocalDatasource {
  // TODO: MiniMax — inject CycleDao, implement CRUD

  const CycleLocalDatasource();
}
