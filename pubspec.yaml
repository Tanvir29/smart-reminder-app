name: smart_reminder_app
description: "ADHD-friendly, privacy-first, modular health assistant."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  go_router: ^14.2.7
  # Database (FFI Stack - The modern standard for Drift + Encryption)
  drift: ^2.16.0
  sqlcipher_flutter_libs: ^0.6.0  # THIS IS THE KEY FOR ENCRYPTION
  path_provider: ^2.1.2
  path: ^1.9.0
  flutter_secure_storage: ^9.2.2
  alarm: ^5.1.5
  flutter_local_notifications: ^18.0.1
  share_plus: ^10.1.4 
  file_picker: ^8.1.6
  uuid: ^4.4.2
  intl: ^0.19.0
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.8
  drift_dev: ^2.16.0
  riverpod_generator: ^2.4.0
  freezed: ^2.4.7
  json_serializable: ^6.7.1
  riverpod_lint: ^2.3.10
  custom_lint: ^0.6.7

flutter:
  uses-material-design: true
  assets:
    - assets/alarms/
