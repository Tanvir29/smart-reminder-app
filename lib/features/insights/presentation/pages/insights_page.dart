/// Page displaying the insights feed.
///
/// MiniMax fills in: `Consumer` watching InsightsNotifier, list of InsightCard
/// widgets, pull-to-refresh to regenerate insights, empty state.
library;

import 'package:flutter/material.dart';

/// Displays generated health insights in a feed layout.
class InsightsPage extends StatelessWidget {
  const InsightsPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement insights feed UI
    return const SizedBox.shrink();
  }
}
