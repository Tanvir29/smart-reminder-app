/// Reminder DTO for database serialization.
///
/// MiniMax fills in: all fields matching the reminders table schema,
/// fromJson/toJson methods for MethodChannel payload serialization.
library;

/// Data transfer object for reminder persistence.
class ReminderModel {
  // TODO: MiniMax — define all fields matching reminders Drift table
  // Include: profile_id, created_at, updated_at, sync_status, xp_value

  const ReminderModel();
}
