/// Local datasource for reminder persistence operations.
///
/// MiniMax fills in: direct Drift DAO calls, query methods.
library;

/// Low-level data access for reminders via Drift.
class ReminderLocalDatasource {
  // TODO: MiniMax — inject AppDatabase or ReminderDao

  const ReminderLocalDatasource();
}
