/// Bottom sheet for confirming a dose was taken.
///
/// MiniMax fills in: medication name, dosage, timestamp picker,
/// optional notes field, confirm/skip buttons, haptic feedback
/// on confirmation.
library;

import 'package:flutter/material.dart';

/// Modal bottom sheet to confirm or skip a medication dose.
class DoseConfirmationSheet extends StatelessWidget {
  const DoseConfirmationSheet({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement dose confirmation bottom sheet
    return const SizedBox.shrink();
  }
}
