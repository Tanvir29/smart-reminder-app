/// Local datasource for medication persistence via Drift DAO.
///
/// MiniMax fills in: CRUD operations delegating to MedicationDao,
/// mapping between MedicationModel and Drift table rows.
library;

/// Handles medication data operations against the local Drift database.
class MedicationLocalDatasource {
  // TODO: MiniMax — inject MedicationDao, implement CRUD

  const MedicationLocalDatasource();
}
