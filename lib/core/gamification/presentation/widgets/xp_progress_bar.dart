/// Widget displaying the user's XP bar and current level.
///
/// MiniMax fills in: animated progress bar, level number,
/// XP count, level-up animation trigger, ADHD-friendly colors.
library;

import 'package:flutter/material.dart';

/// Displays current XP progress toward the next level.
class XpProgressBar extends StatelessWidget {
  const XpProgressBar({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement XP progress bar widget
    return const SizedBox.shrink();
  }
}
