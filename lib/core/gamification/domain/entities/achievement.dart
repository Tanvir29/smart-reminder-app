/// Achievement domain entity.
///
/// MiniMax fills in: fields (id, name, description, iconName,
/// requiredXp, unlockedAt), achievement types.
library;

/// Represents an unlockable achievement in the gamification system.
class Achievement {
  // TODO: MiniMax — define fields per v2 gamification spec

  const Achievement();
}
