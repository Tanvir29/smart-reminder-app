/// Use case: Add a new medication and auto-create reminders.
///
/// MiniMax fills in: call() that persists medication AND creates
/// reminders via the Reminder Engine for all scheduled times.
library;

/// Creates a medication and its associated reminders.
class AddMedication {
  // TODO: MiniMax — inject MedicationRepository + ScheduleReminder use case

  const AddMedication();
}
