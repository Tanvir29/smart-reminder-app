/// Card widget for displaying a single insight.
///
/// MiniMax fills in: icon by type, title, body preview,
/// severity indicator, dismiss/read actions, ADHD-friendly styling.
library;

import 'package:flutter/material.dart';

/// Displays a single insight with type indicator and actions.
class InsightCard extends StatelessWidget {
  const InsightCard({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement insight card UI
    return const SizedBox.shrink();
  }
}
