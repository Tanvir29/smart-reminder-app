/// Medication DTO for database serialization.
///
/// MiniMax fills in: fields matching the medications Drift table.
library;

/// Data transfer object for medication persistence.
class MedicationModel {
  // TODO: MiniMax — define fields matching medications Drift table

  const MedicationModel();
}
