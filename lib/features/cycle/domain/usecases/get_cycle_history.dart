/// Use case: Retrieve cycle history for a date range.
///
/// MiniMax fills in: call() that fetches entries from CycleRepository
/// within the specified range, sorted by date.
library;

/// Fetches cycle entries within a given date range.
class GetCycleHistory {
  // TODO: MiniMax — inject CycleRepository

  const GetCycleHistory();
}
