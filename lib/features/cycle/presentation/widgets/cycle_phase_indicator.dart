/// Widget showing the current cycle phase and prediction.
///
/// MiniMax fills in: phase indicator (menstrual, follicular, ovulation,
/// luteal), days until next period, confidence badge.
library;

import 'package:flutter/material.dart';

/// Displays current cycle phase and prediction summary.
class CyclePhaseIndicator extends StatelessWidget {
  const CyclePhaseIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement cycle phase indicator widget
    return const SizedBox.shrink();
  }
}
