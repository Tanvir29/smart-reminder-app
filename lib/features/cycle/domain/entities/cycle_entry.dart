/// Cycle entry domain entity.
///
/// MiniMax fills in: fields (id, profileId, date, flowIntensity,
/// symptoms, mood, notes, timestamps), copyWith, equality.
library;

/// Immutable cycle entry entity representing a single day's log.
class CycleEntry {
  // TODO: MiniMax — define all fields per v2 cycle spec
  // Must include: profile_id, created_at, updated_at, sync_status, xp_value

  const CycleEntry();
}
