/// Use case: Evaluate and update streak status.
///
/// MiniMax fills in: call() that checks last action timestamp,
/// applies 36-hour ADHD grace period, updates streak count,
/// handles streak freeze/break logic.
library;

/// Evaluates and maintains streak continuity with ADHD grace period.
class EvaluateStreak {
  // TODO: MiniMax — inject GamificationRepository

  const EvaluateStreak();
}
