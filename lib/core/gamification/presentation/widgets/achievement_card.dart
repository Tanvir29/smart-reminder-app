/// Widget displaying an achievement card with unlock status.
///
/// MiniMax fills in: icon, title, description, locked/unlocked state,
/// unlock animation, progress toward achievement criteria.
library;

import 'package:flutter/material.dart';

/// Displays a single achievement with its unlock status.
class AchievementCard extends StatelessWidget {
  const AchievementCard({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement achievement card widget
    return const SizedBox.shrink();
  }
}
