/// Page displaying all active medications with add button.
///
/// MiniMax fills in: `Consumer` watching MedicationNotifier, ListView of
/// MedicationTile, FAB to navigate to AddMedicationPage, pull-to-refresh.
library;

import 'package:flutter/material.dart';

/// Lists all active medications for the current profile.
class MedicationListPage extends StatelessWidget {
  const MedicationListPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: MiniMax — implement medication list UI
    return const SizedBox.shrink();
  }
}
